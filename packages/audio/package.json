{
  "name": "@shape-sound/audio",
  "version": "0.1.0",
  "private": true,
  "description": "⭐ מקור הצליל + מיקס. הנתיב הראשי (\".\") חסין-סביבה: אין window/document/fs/process, תלוי רק ב-core/shared. נתיב-משנה \"./server\" הוא היוצא מן הכלל המתועד: node-web-audio-api, ל-apps/worker בלבד — לעולם לא מיוצא מה-index הראשי, כדי ש-apps/web לא ייגע בו (§3).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/render/serverRenderer.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@shape-sound/core": "workspace:*",
    "@shape-sound/shared": "workspace:*",
    "tone": "^15.1.22",
    "node-web-audio-api": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
