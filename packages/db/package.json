{
  "name": "@soundiform/db",
  "version": "0.1.0",
  "private": true,
  "description": "סכימה ומיגרציות (Drizzle ORM על Supabase Postgres). ראה docs/PROJECT.md §6.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed-genres": "tsx --env-file=.env src/seed/run.ts",
    "db:seed-legal": "tsx --env-file=.env src/seed/run-legal.ts"
  },
  "dependencies": {
    "@soundiform/core": "workspace:*",
    "@soundiform/genres": "workspace:*",
    "@soundiform/shared": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.23.12",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
