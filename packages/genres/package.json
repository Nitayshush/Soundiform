{
  "name": "@shape-sound/genres",
  "version": "0.1.0",
  "private": true,
  "description": "⭐ Genre Packs — נתונים, לא קוד. כל סגנון = JSON אחד. ראה docs/PROJECT.md §5.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@shape-sound/core": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
